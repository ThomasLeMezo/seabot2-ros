observer:
  voice_text:
    ros__parameters:
      mean_target_velocity_v500: 35.0 # kts (always add decimal point)

  height_audio:
    ros__parameters:
      height_too_low: 0.3
      height_high: 0.5
